spring:
  application:
    name: service-pay
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: soms
      config:
        import-check:
          enabled: false
server:
  port: 8040